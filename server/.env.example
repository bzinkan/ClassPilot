# Server Sentry DSN (leave blank for local dev)
SENTRY_DSN_SERVER=

# Google OAuth refresh token encryption key (32 bytes, base64 recommended)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
GOOGLE_OAUTH_TOKEN_ENCRYPTION_KEY=
